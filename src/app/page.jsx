// Descripción: Es la página inicial por defecto de la aplicación, allí se presenta una muy breve descripción del propósito
// de la aplicación y ciertas secciones que dan lugar a la navegación entre páginas

import React from "react";
import Init from "@/components/Init";

const Home = () => {
  return <Init />;
};

export default Home;
